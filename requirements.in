# Top-level runtime dependencies. `requirements.txt` is a hash-locked
# resolution of this file; never install from here directly. Regenerate the
# lockfile with:
#
#   docker run --rm -u "$(id -u):$(id -g)" -e HOME=/tmp \
#       -v "$PWD:/w" -w /w \
#       python:3.12-slim@sha256:804ddf3251a60bbf9c92e73b7566c40428d54d0e79d3428194edf40da6521286 \
#       sh -c 'pip install --quiet --user pip-tools==7.4.1 && \
#              /tmp/.local/bin/pip-compile --generate-hashes --allow-unsafe \
#                  --output-file=requirements.txt requirements.in'

aiosmtpd==1.4.6
PyYAML==6.0.2
httpx==0.28.1
